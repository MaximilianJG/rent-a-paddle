<!-- SUB NAV BAR -->
<div class="navbar navbar-expand-sm navbar-light navbar-lewagon navbar-index">
  <ul class="sub-nav-list">
    <li class="sub-nav-list-item">
      <p>FILTER BY </p>
    </li>
    <li class="sub-nav-list-item">
      <p>CATEGORY</p>
    </li>
    <li class="sub-nav-list-item">
      <p>PRICE</p>
    </li>
    <li class="sub-nav-list-item">
      <p>PERSON</p>
    </li>
  </ul>
</div>

<div class="container mt-5">

  <p class="text-right"> <%= @boats.count %> results for "QUERY" in "LOCATION"</p>

  <div class="row border-top-cards">
    <% @boats.each do |boat| %>
    <div class="col-sm-4">
      <div class="card-boat">
        <%= link_to boat_path(boat) do %>
        <% if boat.photo.attached? %>
          <%= cl_image_tag boat.photo.key, height: 300, width: 400, crop: :fill, class: "card-image" %>
        <% else %>
          <%= cl_image_tag 'download-2_buhn0o', height: 300, width: 400, crop: :fill, class: "card-image" %>
        <% end %>
        <div class="card-boat-infos">
          <div>
            <h2><%= boat.name %></h2>
            <p><%= boat.description %></p>
          </div>
          <h2 class="card-boat-pricing"><%= boat.price %> € / Day </h2>
          <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-boat-user avatar-bordered" />
        </div>
        <% end %>
      </div>
    </div>
    <% end %>
  </div>

</div>

<div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>




