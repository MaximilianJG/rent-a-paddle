<div class="container mt-3">
  <h1>All Boats</h1>

  <ul>
    <% @boats.each do |boat| %>
    <li><%= boat.name %></li>
    <% end %>
  </ul>

  <% if policy(Boat).create? %>
  <%= link_to "New boat", new_boat_path %>
<% end %>


</div>

