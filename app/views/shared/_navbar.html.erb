<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <%= link_to "#", class: "navbar-menu" do %>
      <h5> <i class="fas fa-bars"></i> Menu</h5>
    <% end %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

        <% if user_signed_in? %>
      <% if params[:controller] == "boats" && params[:action] == "index" %> <!--- so was geht?? --->
        <%= render "shared/search_bar" %>
      <% end %>


  </div>



        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">

        <div class="align-items-center">
          <li class="nav-item active">
            <%= link_to "Home", root_path, class: "nav-link" %>
          </li>

          <div class="align-items-center">
            <li>
              <p class="avatar-name-text"> @<%= current_user.username %> </p>
            </li>
            <li class="nav-item dropdown">
              <%= cl_image_tag("AvatarTest_azb3ed", class: "avatar dropdown-toggle mr-2", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false) %>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                <%= link_to "See my Bookings", bookings_path, class: "dropdown-item" %>
                <%= link_to "Account", edit_user_registration_path, class: "dropdown-item" %>
                <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
              </div>
            </li>
          </div>
        </div>

        <%= link_to new_boat_path, class: "nav-item-create-a-new-button" do %>
          <h5 class="nav-link-create-a-new-button">CREATE A NEW BOAT</h5>
        <% end %>
    <% else %>
        <%= link_to new_user_session_path, class: "nav-item-login" do %>
          <h5 class="nav-link-login">LOGIN</h5>
        <% end %>
    <% end %>
    </ul>
  </div>
</div>
